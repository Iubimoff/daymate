CREATE TABLE IF NOT EXISTS timers (
    id TEXT PRIMARY KEY,
    created INTEGER NOT NULL,
    date_end INTEGER NOT NULL,
    hours INTEGER,
    minutes INTEGER,
    seconds INTEGER,
    title TEXT,
    description TEXT
);